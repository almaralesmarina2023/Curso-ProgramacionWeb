import { MayusculasDomDirective } from './mayusculas-dom.directive';

describe('MayusculasDomDirective', () => {
  it('should create an instance', () => {
    const directive = new MayusculasDomDirective();
    expect(directive).toBeTruthy();
  });
});
